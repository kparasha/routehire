# Demo script (private — not in product nav)

Use `/press` only if you need a one-pager in-browser. Prefer this script for the 5-min video.

1. **Driver lead-gen** (`/`) — Uber Freight–style prefs (Home daily / CDL) → live seat count → ZIP → single CTA  
2. **Intake** — T&C opt-in default → questions → **Profile submitted** (expect a call; interviewing agent coming soon)  
3. **Hiring** (`/hauler`) — demand seats + shortlist + contingent fee  
4. **MCP** (`/hauler/mcp`) — paste Cursor config, click `search_jobs` / `list_shortlist`

## UX research takeaway
Best freight UX: **Uber Freight**. Best CDL *job* UX: **Lanefinder/LMDR**. See `openspec/UX-RESEARCH.md`.
